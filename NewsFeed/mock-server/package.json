{
  "name": "newsfeed-mock-server",
  "version": "1.0.0",
  "description": "Mock API server for News Feed Android app",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 3000 --host 0.0.0.0",
    "start:local": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "keywords": [
    "mock",
    "api",
    "json-server",
    "newsfeed"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
